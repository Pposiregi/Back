spring:
  datasource:
    url: jdbc:mysql://localhost:3307/fitpet?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: fitpet1234
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
  security:
    user:
      name: test
      password: test
    oauth2:
      client:
        registration:
          google:
            client-id: test-google-client-id
            client-secret: test-google-client-secret
          kakao:
            client-id: test-kakao-client-id
            client-secret: test-kakao-client-secret

jwt:
  access:
    secret: test-access-secret-key-for-integration-test-only
    expiration-ms: 3600000
  refresh:
    secret: test-refresh-secret-key-for-integration-test-only
    expiration-ms: 604800000

cloud:
  aws:
    credentials:
      access-key: test-access-key
      secret-key: test-secret-key

fcm:
  service-account-key-path: /tmp/test-fcm.json
